from setuptools import setup

setup(name='distributions_gunjan_test',
      version='0.1',
      description='Gaussian distributions and Binomial distribution',
      packages=['distributions_gunjan_test'],
      zip_safe=False)
