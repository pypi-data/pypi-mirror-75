from setuptools import setup

setup(name='distributions_05_dsnd',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['distributions_05_dsnd'],
      zip_safe=False)
