from setuptools import setup

setup(name='dho_distributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['dho_distributions'],
      author='Dan Hernandez',
      zip_safe=False)
