from setuptools import setup

setup(name='matvarg_distributions',
      version='1.1',
      description='Gaussian and Binomial distributions',
      packages=['matvarg_distributions'],
      zip_safe=False)
