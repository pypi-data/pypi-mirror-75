from setuptools import setup

setup(name='limap',
      version='0.1',
      description='LimerBoy Mapper package, https://github.com/LimerBoy/PyMapper/',
      packages=['limap'],
      author_email='clerksmo@protonmail.ch',
      zip_safe=False)