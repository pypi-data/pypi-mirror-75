[tool.poetry]
name = "sentile"
version = "0.2.1"
description = "Sentinel tile utilities"
authors = ["Robofarm <hello@robofarm.io>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"

[tool.poetry.dev-dependencies]

[tool.poetry.scripts]
sentile = "sentile.scripts.__main__:main"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
