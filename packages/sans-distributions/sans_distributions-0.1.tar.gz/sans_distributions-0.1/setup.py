from setuptools import setup

setup(name='sans_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['sans_distributions'],
      zip_safe=False)
