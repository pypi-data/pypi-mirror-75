from setuptools import setup

setup(name='probably-mike',
      version='0.1',
      description='Probability distributions, including Gaussian and Binomial',
      packages=['probably-mike'],
      author='Mike Ajala',
      zip_safe=False)
