from setuptools import setup

setup(name='distributions_pkg_rename',
      version='0.2',
      description='Gaussian distributions',
      packages=['distributions_pkg_rename'],
      zip_safe=False)
