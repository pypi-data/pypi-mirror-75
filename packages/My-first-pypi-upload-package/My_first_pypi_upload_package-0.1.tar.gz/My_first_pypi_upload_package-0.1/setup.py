from setuptools import setup

setup(name='My_first_pypi_upload_package',
      version='0.1',
      description='Gaussian distributions',
      packages=['My_first_pypi_upload_package'],
      zip_safe=False)
