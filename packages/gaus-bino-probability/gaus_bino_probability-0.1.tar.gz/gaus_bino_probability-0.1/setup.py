from setuptools import setup

setup(name='gaus_bino_probability',
      version='0.1',
      description='Gaussian And Binomial distributions',
      packages=['gaus_bino_probability'],
      author='Nishant Gupta',
      zip_safe=False)
