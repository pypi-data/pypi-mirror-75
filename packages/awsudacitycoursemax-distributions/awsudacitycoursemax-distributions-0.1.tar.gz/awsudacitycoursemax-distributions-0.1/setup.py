from setuptools import setup

setup(name='awsudacitycoursemax-distributions',
      version='0.1',
      description='Gaussian and binomial distributions',
      packages=['distributions'],
      author=['Maximilian Muench'],
      zip_safe=False)
