from setuptools import setup

setup(name='distributions_dsnd_pa',
      version='0.1',
      description='Gaussian amd Binomial distributions',
      packages=['distributions_dsnd_pa'],
      zip_safe=False)
