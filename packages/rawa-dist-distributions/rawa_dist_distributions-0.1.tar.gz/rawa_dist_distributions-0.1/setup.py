from setuptools import setup

setup(name='rawa_dist_distributions',
      version='0.1',
      description='Gaussian and Binomial Distributions',
      packages=['rawa_dist_distributions'],
      zip_safe=False)
