[tool.poetry]
name = "setup_utils"
version = "0.2.0"
description = "Python Setup Utilities"
authors = ["skeptycal <26148512+skeptycal@users.noreply.github.com>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.8"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
