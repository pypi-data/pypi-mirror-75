from setuptools import setup

setup(name='demodistributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['demodistributions'],
      author='Tousifahamed',
      zip_safe=False)
