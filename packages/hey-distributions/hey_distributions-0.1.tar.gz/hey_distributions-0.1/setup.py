from setuptools import setup

setup(name='hey_distributions',
      version='0.1',
      description='Binomial and Gaussian distributions',
      packages=['hey_distributions'],
      zip_safe=False)
