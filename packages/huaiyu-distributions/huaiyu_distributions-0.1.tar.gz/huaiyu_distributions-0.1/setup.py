from setuptools import setup

setup(name='huaiyu_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['huaiyu_distributions'],
      zip_safe=False)
