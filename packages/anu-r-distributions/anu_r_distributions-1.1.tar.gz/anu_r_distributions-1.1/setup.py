from setuptools import setup

setup(name='anu_r_distributions',
      version='1.1',
      description='Gaussian and binomial distributions',
      packages=['anu_r_distributions'],
      author='Anubhav Ranjan',
      zip_safe=False)
