from setuptools import setup

setup(name='distributions-smd',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions-smd'],
      author = 'Sandra',
      author_email = 'sandra@email.com',
      zip_safe=False)
