from setuptools import setup

setup(name='special_distributions',
      version='0.1',
      description='Gaussian distributions',
      author = 'Mohamed Osama',
      packages=['special_distributions'],
      zip_safe=False)
