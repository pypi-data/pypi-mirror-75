from setuptools import setup

setup(name='my_first_distributions_15',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['my_first_distributions_15'],
      zip_safe=False)
