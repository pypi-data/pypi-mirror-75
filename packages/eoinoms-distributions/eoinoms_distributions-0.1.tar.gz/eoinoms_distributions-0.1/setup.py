from setuptools import setup

setup(name='eoinoms_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['eoinoms_distributions'],
      zip_safe=False)
