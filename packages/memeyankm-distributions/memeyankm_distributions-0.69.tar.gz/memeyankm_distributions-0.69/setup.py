from setuptools import setup

setup(name='memeyankm_distributions',
      version='0.69',
      description='Gaussian and Binomial distributions',
      packages=['memeyankm_distributions'],
      author="memeyankm",
      zip_safe=False)
