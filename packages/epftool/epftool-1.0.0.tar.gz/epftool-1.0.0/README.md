# EPF tool
Command line tool to search EPF files.  
## Run from pip
`pip install epftool`  
`epftool --help`  
## Run from source
Install pipenv.  
`pipenv install`  
`pipenv run python epftool --help`  
