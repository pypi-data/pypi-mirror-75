from setuptools import setup

setup(name='khachhibhoya_distributions',
      version='1.1',
      description='Gaussian and binomial distributions',
      packages=['distributions'],
      author="neha",
      zip_safe=False)
