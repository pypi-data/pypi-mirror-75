#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['twitchdetox']

package_data = \
{'': ['*']}

install_requires = \
['requests>=2.24.0', 'irc>=18.0.0']

entry_points = \
{'console_scripts': ['detox = twitchdetox.bot:main',
                     'detox-settings = twitchdetox.settings:main']}

setup(name='twitchdetox',
      version='0.0.0',
      description='Automatic Twitch moderation tool to filter out toxicity and harassment.',
      author='Fitti',
      author_email='dev@fitti.io',
      url='https://github.com/Fittiboy/twitch-detox',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      entry_points=entry_points,
      python_requires='>=3.8',
     )
