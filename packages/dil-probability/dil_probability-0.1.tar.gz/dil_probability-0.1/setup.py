from setuptools import setup

setup(name='dil_probability',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['dil_probability'],
      author='Dil',
      zip_safe=False)
