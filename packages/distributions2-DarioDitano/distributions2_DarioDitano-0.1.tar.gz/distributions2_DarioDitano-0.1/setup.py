from setuptools import setup

setup(name='distributions2_DarioDitano',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions2_DarioDitano'],
      zip_safe=False)
