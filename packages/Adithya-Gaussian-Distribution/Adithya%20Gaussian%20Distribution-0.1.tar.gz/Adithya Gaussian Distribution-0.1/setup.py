from setuptools import setup

setup(name='Adithya Gaussian Distribution',
      version='0.1',
      description='Gaussian distributions',
      packages=['Adithya Gaussian Distribution'],
      zip_safe=False)
