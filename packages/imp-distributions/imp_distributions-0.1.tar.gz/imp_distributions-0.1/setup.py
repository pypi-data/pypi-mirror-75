from setuptools import setup

setup(name='imp_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['imp_distributions'],
      zip_safe=False)
