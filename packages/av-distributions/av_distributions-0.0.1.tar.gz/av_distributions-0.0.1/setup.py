from setuptools import setup

setup(name='av_distributions',
      version='0.0.1',
      description='Gaussian and Binomial distributions',
      packages=['av_distributions'],
      zip_safe=False)
