from setuptools import setup

setup(name='distributions_binomialgaussian',
      version='0.1',
      description='Gaussian  and binomial distributions',
      packages=['distributions_binomialgaussian'],
      zip_safe=False)
