from setuptools import setup

setup(name='distributions_2020_0.1',
      version='0.1',
      description='Gaussian + Binomial distributions',
      packages=['distributions'],
      Author="Altaf Pandhare",
      zip_safe=False)

