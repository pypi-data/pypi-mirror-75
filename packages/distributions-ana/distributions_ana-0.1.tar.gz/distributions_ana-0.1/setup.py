from setuptools import setup

setup(name='distributions_ana',
      version='0.1',
      description='Gaussian and binomial distributions',
      packages=['distributions_ana'],
      author = 'Ananth',
      zip_safe=False)
