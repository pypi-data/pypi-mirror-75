from setuptools import setup

setup(name='statipy317_distributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['statipy317_distributions'],
      zip_safe=False)
