from setuptools import setup

setup(name='probability_distributions07',
      version='0.1',
      description='Gaussian distributions',
      packages=['probability_distributions07'],
      zip_safe=False)
