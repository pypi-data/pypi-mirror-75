from setuptools import setup

setup(name='distributions_bindupillai',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions_bindupillai'],
      zip_safe=False)
