from setuptools import setup

setup(name='punj_distributions',
      version='0.3',
      description='Gaussian & Binomial distributions',
      packages=['punj_distributions'],
      zip_safe=False)
