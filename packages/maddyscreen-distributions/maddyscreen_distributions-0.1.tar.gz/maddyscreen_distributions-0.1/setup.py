from setuptools import setup

setup(name='maddyscreen_distributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['maddyscreen_distributions'],
      zip_safe=False)
