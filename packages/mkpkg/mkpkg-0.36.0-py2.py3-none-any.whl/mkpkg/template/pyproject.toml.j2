[build-system]
requires = [{% if cffi %}
    "cffi>=1.0.0",{%- endif %}
    "setuptools>=40.6.0",
    "setuptools_scm[toml]>=3.4",
    "wheel",
]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
