from setuptools import setup

setup(name='arkadip_dsnd_distributions',
      version='0.1',
      description='Arkadip"s Gaussian distributions',
      packages=['distributions'],
      zip_safe=False)
