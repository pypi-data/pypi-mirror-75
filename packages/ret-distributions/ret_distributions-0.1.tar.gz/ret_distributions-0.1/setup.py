from setuptools import setup

setup(name='ret_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['ret_distributions'],
      zip_safe=False)
